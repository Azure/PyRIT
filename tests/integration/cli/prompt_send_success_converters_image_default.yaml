# scenario: verify converters (that take image input) functionality with all converters as of v0.8.1
datasets:
  - ./tests/integration/cli/illegal_integration_test_image_input.prompt
scenarios:
  - type: "PromptSendingOrchestrator"
objective_target:
  type: "OpenAIChatTarget"
converters:
  - type: "AddTextImageConverter"
    text_to_add: "test"
  # - type: "AddImageVideoConverter" # TODO: Unsure why this fails when uncommented, but just adding the above converter works right now :-)
  #   video_path: "./assets/sample_video.mp4"
database:
  type: "DuckDB"
